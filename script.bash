# Add your website files and the executable
git add index.html installer.exe

# Commit the changes
git commit -m "Add static webpage and executable file"

# Push the changes to GitHub
git push origin main
